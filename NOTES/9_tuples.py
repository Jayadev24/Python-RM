# TUPLES in Python : A built-in data type that lets us create "IMMUTABLE" sequences of values.

tuple = (87, 64, 33, 95, 76)
print(type(tuple))

# tup[0] = 43 item assignment is NOT allowed in tuples

tup1 = ()

tup2 = ( 1, )   # we use , to show the type as tuple

tup3 = ( 1, 2, 3 )

#

tup = (2, 1, 3, 1)

tup.index( 1 ) #returns index of first occurrence    tup.index(1) is 1

tup.count( 1 ) #counts total occurrences             tup.count(1) is 2